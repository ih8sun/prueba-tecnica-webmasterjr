/**
 * Escribir una función llamada distancia que reciba dos strings y retorne el número de caracteres diferentes 
 * (comparando posición por posición).

    Nota: Puedes asumir que los strings siempre tienen la misma longitud.
    
    Ejemplo:
    funcion(param1,param2)

    distancia('hola','hola') // 0
    distancia('hola','Hola') // 1
    distancia('hola','hila') // 1
    distancia('hola','adios') // 4
    distancia('sol','tol') // 1
    distancia('carsor','correr') // 4
 */

function distancia(str1, str2) {

    console.log('AQUI DEBERIA IR TU SOLUCION')
}

distancia('hola', 'hola') // 0


